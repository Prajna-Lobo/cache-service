package constants

const ConfigFileName = "./configuration/config.json"

const (
	DefaultInterval = 5
	CleanupInterval = 10
	CacheExpiration = 30
)

const (
	PageNumber = "page_num"
	PageSize = "page_size"
)
